# Train → Inference

Ban day du stack (PyTorch, TensorFlow, HF, Kaggle, GPU online): **[train-gpu.md](./train-gpu.md)**.

```
data → train (epochs on GPU) → checkpoint → load → infer
```

Browser demos = minh hoa inference. Train nang o notebook protonx / cloud GPU.
